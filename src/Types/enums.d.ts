export enum GameMode {
  NORMAL,
  TIME_HUNT
}

export enum CardElement {
  FIRE = 'Fire',
  WATER = 'Water',
  ENERGY = 'Energy',
}

export enum CardColor {
  RED = 'Red',
  BLUE = 'Blue',
  YELLOW = 'Yellow',
}

export enum CardAmount {
  ONE = 1,
  TWO,
  THREE
}
